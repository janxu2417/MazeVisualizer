"""Entry point for MazeVisualizer — launch via ``python src/main.py``."""

from app import run_app


def main() -> None:
    """Start the MazeVisualizer application."""
    run_app()


if __name__ == "__main__":
    main()
